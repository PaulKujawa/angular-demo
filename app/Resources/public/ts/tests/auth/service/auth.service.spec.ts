import {AuthService} from "../../../app/core/service/auth.service";
// import {Jwt} from "../../../app/auth/model/jwt";

describe('AuthService', () => {
    // let service: AuthService;
    //
    // beforeEach(() => {
    //     service = new AuthService();
    // });
    //
    // it('#getAuthorizationHeader should return correct format', () => {
    //     const jwt = new Jwt('token');
    //     service.setJwt(jwt);
    //
    //     expect(service.getAuthorizationHeader()).toBe('Bearer {token}');
    // });
    //
    // it('#getAuthorizationHeader should return null when no token is set', () => {
    //     expect(service.getAuthorizationHeader()).toBeNull();
    // });
});

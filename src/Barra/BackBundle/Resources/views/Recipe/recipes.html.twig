{% extends 'BarraBackBundle::layout.html.twig' %}

{% block title %} vPit Recipes {% endblock %}

{% block section %}
    {{ form(formNew) }}


    <h1>all recipes</h1>

    <ul>
    {% for recipe in recipes %}
        <li>
            <a href="{{ path('barra_back_recipe', {'id': recipe.id}) }}">{{ recipe.name }}</a>
        </li>
    {% endfor %}
    </ul>

{% endblock %}
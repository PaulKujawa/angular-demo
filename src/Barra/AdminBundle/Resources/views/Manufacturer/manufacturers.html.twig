{% extends 'BarraAdminBundle::layout.html.twig' %}
{% form_theme form 'BarraAdminBundle:Form:tableRow.html.twig' %}
{% block title %}{{ 'admin.title.manufacturers'|trans() }}{% endblock %}

{% block section %}
    <section class="container mobileTables" ng-controller="ManufacturerCtrl">
        {{ include('BarraAdminBundle:parts:formValidation.html.twig') }}

        {{ form_start(form) }}
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>{{ 'admin.manufacturer.name'|trans() }}</th>
                        <th>{{ 'admin.action.delete'|trans() }}</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="manufacturer in manufacturers | orderBy:'name'">
                        <td data-mobile-title={{ 'admin.manufacturer.name'|trans() }}>[[manufacturer.name]]</td>
                        <td data-mobile-title={{ 'admin.action.delete'|trans() }}>
                            <span class="glyphicon glyphicon-trash" ng-click="deleteEntry(manufacturer)"></span>
                        </td>
                    </tr>
                    <tr class="formInRow">
                        {{ form_row(form.name) }}
                        {{ form_widget(form.submit, {'label': 'admin.action.new', 'attr': {'class': 'btn'} }) }}
                    </tr>
                </tbody>
            </table>
        {{ form_end(form) }}

        {{ include('BarraAdminBundle:parts:pagination.html.twig', { 'route': 'barra_admin_manufacturers'})}}
    </section>
{% endblock %}